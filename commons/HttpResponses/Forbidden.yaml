description: Forbidden
content:
  application/json:
    schema:
      $ref: './HttpError.yaml'
    examples:
      objectExample:
        $ref: '#/components/examples/Forbidden'

components:
  examples:
    Forbidden:
      value:
        statusCode: '403'
        status: 'FAILED'
        message: 'Forbidden'
