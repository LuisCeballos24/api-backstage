type: array
items:
    properties:
        Address:
            $ref: './Address.yaml'
        organisationLocationType:
            type: string
            description: Valores posibles para el tipo de ubicación de la organización
            enum:
                - PlaceOfRegistration
                - PlaceOfOperation
        LocationReference:
            type: object
            description: referente a la localizacion
        OrganisationReference:
            type: object
            description: referente a la organización
        locationDescription:
            type: string
